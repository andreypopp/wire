* Provide module format compatible with AMD, CommonJS and browser globals.
  See https://github.com/umdjs/umd for examples. Use preprocessor for that.

* Integrate jslint (or jshint?).

* Try to use closure compiler for type checking.

* Try to provide builds with uglify-js, closure compiler with different
  compilation levels.
